<h1><%= @task.title %></h1>

<h3><%= @task.details %></h3>
<p>Completed - <%=  @task.completed %></p>

<p><%= link_to "Edit", edit_task_path(@task) %></p>

<p>⚠️<%= link_to "Delete #{@task.name}", task_path(@task), method: :delete, data: { confirm: "Are you sure?" } %></p>
